{
  "name": "uri-template",
  "version": "1.0.0",
  "description": "Use template strings to safely build URIs",
  "main": "index.js",
  "scripts": {
    "format": "prettier --write .",
    "build": "tsc",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "uri",
    "template",
    "tag_function"
  ],
  "author": "pauloo27",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.2"
  }
}
